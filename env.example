# Порт для Caddy (reverse proxy)
# На Render используйте PORT=80 (Caddy слушает на 80, проксирует на Go app)
# Локально можно использовать PORT=8080 если запускаете без Caddy
PORT=8080

# Порт для Go приложения (внутренний, используется Caddy)
# В Docker всегда 8080, менять не нужно
# APP_PORT=8080

# Базовый URL для формирования коротких ссылок
# Development: http://localhost:8080
# Production: https://your-app.onrender.com
BASE_URL=http://localhost:8080

# URL базы данных PostgreSQL (ОБЯЗАТЕЛЬНО)
# Format: postgres://user:password@host:port/database?sslmode=disable
DATABASE_URL=postgres://postgres:password@localhost:5432/urlshortener?sslmode=disable

# Sentry DSN для мониторинга ошибок (опционально)
# Получите DSN на https://sentry.io/
SENTRY_DSN=

# Окружение (development/production, default: development)
ENV=development

# URL фронтенда для CORS (опционально, только для production)
# В development автоматически используется http://localhost:5173
FRONTEND_URL=

# Для тестов (опционально)
TEST_DATABASE_URL=postgres://postgres:password@localhost:5432/urlshortener_test?sslmode=disable
