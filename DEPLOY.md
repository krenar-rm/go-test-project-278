# Инструкция по деплою на Render с UI

## Подготовка

### 1. PostgreSQL база данных на Render

Если еще не создана, создайте PostgreSQL базу данных:

1. Откройте [Render Dashboard](https://dashboard.render.com/)
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Выберите имя: `url-shortener-db`
4. Region: выберите ближайший
5. Instance Type: **Free**
6. Нажмите **"Create Database"**
7. Скопируйте **Internal Database URL** (начинается с `postgres://`)

### 2. Sentry DSN (опционально)

Ваш Sentry DSN:
```
https://88a6dba7b1bff0ef7a9a4606a3cd1297@o4510353669423104.ingest.de.sentry.io/4510353673748560
```

### 3. Пуш кода в GitHub

Убедитесь, что все изменения закоммичены и запушены:

```bash
git add .
git commit -m "Add UI support with Caddy proxy"
git push origin main
```

## Деплой на Render

### Шаг 1: Создание Web Service

1. Откройте [Render Dashboard](https://dashboard.render.com/)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите репозиторий `krenar-rm/go-test-project-278`

### Шаг 2: Настройка сервиса

Заполните поля:

| Поле | Значение |
|------|---------|
| **Name** | `url-shortener` (или другое на ваш выбор) |
| **Region** | Выберите ближайший регион |
| **Branch** | `main` или `master` |
| **Language** | `Docker` |
| **Instance Type** | `Free` |

### Шаг 3: Переменные окружения

⚠️ **ВАЖНО:** Обратите внимание на PORT=80 (не 8080)!

Добавьте следующие переменные окружения:

```
PORT=80
ENV=production
BASE_URL=https://your-app-name.onrender.com
DATABASE_URL=<ваш Internal Database URL из шага 1>
SENTRY_DSN=https://88a6dba7b1bff0ef7a9a4606a3cd1297@o4510353669423104.ingest.de.sentry.io/4510353673748560
```

**Как добавить:**
1. Прокрутите вниз до секции "Environment Variables"
2. Нажмите "Add Environment Variable"
3. Добавьте каждую переменную отдельно

**Важные моменты:**
- `PORT=80` - Caddy слушает на порту 80
- `BASE_URL` - замените на реальный URL вашего приложения
- `DATABASE_URL` - используйте Internal Database URL из Render PostgreSQL

### Шаг 4: Деплой

1. Нажмите **"Create Web Service"**
2. Render начнет сборку:
   - Соберет frontend (Node.js)
   - Соберет backend (Go)
   - Создаст runtime образ с Caddy
   - Запустит приложение
   - Запустит контейнер

Процесс займет 5-10 минут.

## Проверка после деплоя

### 1. Получите URL приложения

После успешного деплоя, Render предоставит URL вида:
```
https://url-shortener-xxxx.onrender.com
```

### 2. Проверьте Web UI

Откройте в браузере:
```
https://url-shortener-xxxx.onrender.com
```

Должен открыться **React Admin интерфейс** с возможностью:
- Просмотра списка ссылок
- Создания новых ссылок
- Редактирования существующих
- Удаления ссылок

### 3. Проверьте Backend API

```bash
# Проверка работоспособности API
curl https://url-shortener-xxxx.onrender.com/ping
# Ответ: pong

# Проверка получения ссылок
curl https://url-shortener-xxxx.onrender.com/api/links
# Ответ: JSON массив со ссылками
```

### 4. Проверьте редирект

Создайте короткую ссылку через UI, затем:
```bash
curl -L https://url-shortener-xxxx.onrender.com/r/your-short-name
# Должен перенаправить на оригинальный URL
```

### 5. Проверьте Sentry (опционально)

1. Откройте [Sentry Dashboard](https://sentry.io/)
2. Перейдите в ваш проект
3. Проверьте поступление событий

## Обновление README

После успешного деплоя обновите ссылку в README.md:

```markdown
## 🔗 Демо

**Развернутое приложение:** https://url-shortener-xxxx.onrender.com
```

Замените `url-shortener-xxxx.onrender.com` на реальный URL.

## Автоматический деплой

Render настроен на автоматический деплой при пуше в главную ветку:

```bash
# Внесите изменения
git add .
git commit -m "Update feature"
git push origin main

# Render автоматически пересоберет и задеплоит
```

## Мониторинг

### Логи приложения

Render Dashboard → Ваш сервис → **Logs**

### Ошибки в Sentry

[Sentry Dashboard](https://o4510353669423104.ingest.de.sentry.io/) → Ваш проект → **Issues**

### CI/CD статус

GitHub → Ваш репозиторий → **Actions**

## Возможные проблемы

### Приложение не запускается

1. Проверьте логи в Render Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что Dockerfile корректен

### Sentry не получает события

1. Проверьте правильность SENTRY_DSN
2. Убедитесь, что переменная окружения установлена в Render
3. Проверьте логи приложения на наличие ошибок инициализации Sentry

### Медленный холодный старт

Free tier Render может "засыпать" после 15 минут неактивности. Первый запрос после этого займет 30-60 секунд.

## Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Render Docker Guide](https://render.com/docs/docker)
- [Sentry Go Documentation](https://docs.sentry.io/platforms/go/)
- [Sentry Gin Integration](https://docs.sentry.io/platforms/go/guides/gin/)

